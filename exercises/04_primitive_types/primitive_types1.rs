// 布尔类型（`bool`）

fn main() {
    let is_morning = true;
    if is_morning {
        println!("早上好！");
    }

    // TODO: 在下面的 `if` 语句前定义一个名为 `is_evening` 的布尔变量。
    // 该变量的值应为 `is_morning` 的逻辑非（相反值）。
    // let …
    let is_evening = false;
    if is_evening {
        println!("晚上好！");
    }
}
